// questo sara il file convertito in Javascript
function DescribeCurrency(countryName, currency) {
    return "the currency of ".concat(countryName, " is the ").concat(currency.name, " (").concat(currency.code, ") ");
}
